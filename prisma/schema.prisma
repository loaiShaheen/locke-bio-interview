datasource db {
  provider = "postgresql"
  url      = "postgres://admin:admin@localhost:5432/nestjs"
}

generator client {
  provider = "prisma-client-js"
}

model Prescription {
  id    String     @id @default(uuid()) @unique
  email String?
  name  String?
  file  Bytes?   @db.ByteA
}